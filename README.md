#Compilations

In a container at the head of create-appimage.sh:

Compile in succession: mimalloc, type2-runtime (taking a header from squashfuse), appimagetool

Then use those to compile the AppImages, rosa-base must be done first
